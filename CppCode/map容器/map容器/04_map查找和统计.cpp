#include"utils.h"

/*
find(key); //查找key是否存在,若存在，返回该键的元素的迭代器；若不存在，返回set.end();
count(key); //统计key的元素个数
*/

void test4()
{
	map<int, int> m;
	m.insert(pair<int, int>(1, 10));

}



int main()
{
	test4();

	system("pause");
	return 0;
}